import { Renderer2StyleDirective } from './renderer2-style.directive';

describe('Renderer2StyleDirective', () => {
  it('should create an instance', () => {
    const directive = new Renderer2StyleDirective();
    expect(directive).toBeTruthy();
  });
});
